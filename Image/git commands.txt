git -v

git config --global user.name "Ivan Samoilov"
git config --global user.email "useremail.com"

git init

git add .

git commit -m "1 file added"

git remote add origin https://github.com/isam2403/firstName.git

git clone https://github.com/zenoamaro/react-quill.git